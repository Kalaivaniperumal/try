{
    "name": "myapp",
    "version": "1.0.0",
    "description": "A simple Node.js application",
    "scripts": {
      "start": "node server.js", 
      "build": "echo 'No build step defined, add one if needed'", 
      "deploy": "npm run build && pm2 restart myapp || pm2 start npm --name 'myapp' -- run start"
    },
    "dependencies": {
      "express": "^4.18.2"
    },
    "devDependencies": {},
    "engines": {
      "node": ">=18.0.0"
    }
  }  